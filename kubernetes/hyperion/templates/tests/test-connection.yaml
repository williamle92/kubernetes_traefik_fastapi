apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hyperion.fullname" . }}-test-connection"
  labels:
    {{- include "hyperion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hyperion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
