apiVersion: v1
kind: Service
metadata:
  name: {{ include "hyperion.fullname" . }}
  labels:
    {{- include "hyperion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hyperion.selectorLabels" . | nindent 4 }}
